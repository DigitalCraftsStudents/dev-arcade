import { createStore } from "redux";

const defaultState = {
    state = {
  game: {
    nextShape: 1,
    rotation: 1,
    grid: [bunch, of, values],
    more, properrties
  }
}

};

export const store = createStore(defaultStatus);
